<% provide :page_title, "New Reservation" %>

<h1>Make a Reservation For </h1>
	<h3><%= @location.restaurant.name.capitalize %></h3>
	<p><%= @location.address %></p>
  <p>Open Hour: <%= @location.operation_hours %></p>
  <p>Available Seats: <%= @location.available_seats %></p>

<div class="form">
  <%= render "form" %>
</div>
